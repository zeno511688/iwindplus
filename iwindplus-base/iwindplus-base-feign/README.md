# feign模块
    一、FeignRequestInterceptor 请求头信息服务传递下去
    二、feign统一异常处理（默认注入）
        1、在配置文件（yml,properties）中配置FeignProperty类中相关属性
    二、feign重试策略配置
        1、在配置文件（yml,properties）中配置FeignProperty类中相关属性
    三、feign支持文件上传表单处理
    四、支持feign调用统一响应解密
