<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.iwindplus.base.async.task.dal.mapper.AsyncTaskMapper">
    <sql id="inSql">
        <foreach collection="list" item="item" index="index" open="(" separator="," close=")">
            #{item}
        </foreach>
    </sql>

    <delete id="deleteDataByIds">
        delete ac, acs
        from async_task ac
        left join async_task_sub acs on acs.async_task_id = ac.id
        where ac.id in
        <include refid="inSql"/>
    </delete>

    <delete id="deleteDataById">
        delete at, ats
        from async_task at
        left join async_task_sub ats on ats.async_task_id = at.id
        where at.id = #{id}
    </delete>

    <delete id="deleteByBizNumber">
        delete at, ats
        from async_task at
        left join async_task_sub ats on ats.async_task_id = at.id
        where at.env = #{env} and at.biz_number = #{bizNumber}
    </delete>
</mapper>
