<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.iwindplus.mgt.server.dal.mapper.power.UserGroupUserMapper">
    <sql id="userRoleAliasField">
        bugu.id, bugu.created_time, bugu.created_timestamp, bugu.created_by, bugu.created_id, bugu.modified_time, bugu.modified_timestamp, bugu.modified_by, bugu.modified_id, bugu.deleted, bugu.version, bugu.remark,
        bugu.user_id, bugu.user_group_id
    </sql>

    <sql id="inSql">
        <foreach collection="list" item="item" index="index" open="(" separator="," close=")">
            #{item}
        </foreach>
    </sql>
    
    <delete id="deleteByIds">
        delete from user_group_user
        where id in
        <include refid="inSql"/>
    </delete>

    <delete id="deleteByUserGroupIds">
        delete from user_group_user
        where user_group_id in
        <include refid="inSql"/>
    </delete>

    <delete id="deleteByUserIds">
        delete from user_group_user
        where id in
        <include refid="inSql"/>
    </delete>
</mapper>
